# AnyBody Modeling System Example model

Example model that shows how to use `AnyMuscleModelUsr1` and `AnyGeneralMuscle` to allow for a time dependent contact between two objects. I.e. a dumbbell and a arm.
